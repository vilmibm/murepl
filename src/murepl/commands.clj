(ns murepl.commands
  (:require [murepl.user :as u]))

;; new
;; login
;; change-password
;; set-info <"key" "val">
;; help

(defn dispatch [username command-str]
  nil)

(defn evaluate [code]
  nil)

(defn login [username password]
  nil)

(defn new [username password]
  nil)

(defn set-info [username k v]
  nil)

(defn change-password [username password]
  nil)

(defn help []
  nil)
